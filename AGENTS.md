# Repository guide

## Architecture and sources of truth

- Django models, serializers, views and `openapi/openapi.yaml` define the backend contract.
- `packages/api-client/src/generated/` is generated from OpenAPI; never edit it by hand.
- Platform-neutral TypeScript belongs in `packages/shared`; web UI belongs in `apps/web`.
- Mobile UI belongs in `apps/mobile` and consumes the canonical `@personal-library/react-native-components` library.
- Generic reusable native UI primitives belong in the personal library. Product/domain UI belongs in the application.
- PostgreSQL is the only supported database. Migrations are committed source files.

## Commands

- Setup: `make setup`; configure a clone with `make init`.
- Run: `make dev`; stop with `make down`; diagnose with `make doctor`.
- Checks: `make lint`, `make typecheck`, `make test`, `make api-check`, `make check`.
- Contract updates: `make api-schema`, then `make api-client`, then `make api-check`.
- Python commands use `uv`; JavaScript commands use pnpm workspaces.

## Boundaries and safety

- `apps/backend` owns HTTP behavior and auth. Browser auth uses HttpOnly session cookies and CSRF; mobile uses short-lived JWT access tokens in Expo SecureStore.
- Web must not import React Native dependencies. Mobile must not import DOM-only packages. Shared packages must remain platform-neutral and Node-free.
- Mobile agents: before creating a generic React Native UI primitive, inspect `@personal-library/react-native-components` first. Use documented root exports only; compose or wrap existing primitives before considering an application-specific fallback.
- Never commit credentials, generated build output, or local `.env` files. Production origins/hosts must be explicit and HTTPS-only.
- Do not auto-create migrations at application startup. Run `make migrations`, review the migration, and test it against PostgreSQL.
- Uploads are untrusted input. Do not serve uploaded content as trusted HTML or executable files.

## Generated files

- `openapi/openapi.yaml`: generated by Django/drf-spectacular.
- `packages/api-client/src/generated/`: generated by Orval from the committed schema.
- Update both only through `make api-schema` and `make api-client`; CI checks for drift.

## Definition of Done

- Acceptance behavior has tests; required lint, typecheck, tests and relevant builds pass.
- API changes update schema and generated client; model changes include reviewed migrations.
- Mobile UI reuses the personal library and passes package resolution, render, Expo doctor and export checks where the environment supports them.
- `git diff --check` is clean and only scoped files changed.

## Subagents

Use subagents for independent read-only exploration, compatibility research, test triage or review when a task has enough scope to justify separate contexts. Keep one owner for writes, assign disjoint file ownership for any explicitly delegated implementation, and require each agent to return evidence and commands. Avoid delegation for small changes or concurrent edits to shared manifests and generated files.
