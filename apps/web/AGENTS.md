# Web instructions

Use accessible DOM elements, React Router and TanStack Query. Consume `@template/api-client`; do not duplicate API models. Browser credentials use `credentials: include` and CSRF for unsafe requests. Never store auth secrets in localStorage. Keep React Native and Expo dependencies out of this workspace. Run web lint, typecheck, Vitest and production build.
